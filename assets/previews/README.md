# Preview GIFs

Animated previews used in the project README and docs. Regenerate after
changing a mobject — see the "Generating preview GIFs" section in the
project README for the full ffmpeg incantation.

| File | Source scene | Notes |
|---|---|---|
| `mtable.gif` | `examples/01_table_basic.py::OrdersTable` | Hero image for `MTable` |
| `mbtree.gif` | `examples/02_btree_search.py::BTreeSearchAndInsert` | Hero image for `MBTree` |

These files are committed because they're small and the README needs them.
Keep them under ~3 MB each — anything larger is slow to load on GitHub.
